do {
    const game = prompt("Здавствуйте. Вы имеете таковы команды.: 'trainingPeople', 'addCompany', 'end'")
    if (game == "end") break
    switch(game){
        case "trainingPeople" : {
            break
        }
        case "addCompany" : {
            break
        }
        default:{
            alert("Commands not found")
        break
        }
    }

} while(true)